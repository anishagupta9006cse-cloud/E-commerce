import React from 'react';

const Navbar = () => {
  return (
    <nav style={{ display: 'flex', justifyContent: 'space-between', padding: '1rem 2rem', background: '#1a1a1a', color: '#fff' }}>
      <h2 style={{ margin: 0 }}>E-Commerce Store</h2>
      <div>
        <a href="/" style={{ color: '#fff', textDecoration: 'none', marginRight: '1rem' }}>Home</a>
        <a href="/cart" style={{ color: '#fff', textDecoration: 'none' }}>Cart</a>
      </div>
    </nav>
  );
};

export default Navbar;